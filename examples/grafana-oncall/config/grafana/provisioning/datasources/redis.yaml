apiVersion: 1

deleteDatasources:
- name: Redis
  uid: redis

datasources:
- name: Redis
  type: redis-datasource
  access: proxy
  orgId: 1
  isDefault: false
  # url: redis://host.docker.internal:6379
  url: redis://redis:6379/0
  jsonData:
    client: standalone
  editable: true
  # secureJsonData:
  #   password: $PASSWORD
