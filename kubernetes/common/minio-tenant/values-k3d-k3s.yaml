tenant:
  name: codelab
  image:
    tag: RELEASE.2025-09-07T16-13-09Z
  configSecret:
    name: codelab-minio-env
  # using certificates generated by cert-manager.
  certificate:
    requestAutoCert: false
    externalCertSecret:
    - name: codelab-tls
      type: cert-manager.io/v1
  pools:
  - name: pool-10gb
    servers: 1
    volumesPerServer: 1
    size: 10Gi

  features:
    bucketDNS: false
    domains: { }
    enableSFTP: false
  
  exposeServices:
    minio: false
    console: false

  buckets:
  - name: mimir-blocks
  - name: mimir-ruler
  - name: mimir-alertmanager
  - name: loki-data
  - name: loki-ruler
  - name: tempo-data
  - name: pyroscope-data
  
  # Each secret should specify the ``CONSOLE_ACCESS_KEY`` and ``CONSOLE_SECRET_KEY`` as the access key and secret key for that user.
  users:
  - name: user-lgtmp